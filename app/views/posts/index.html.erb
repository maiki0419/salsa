<div class="container">
  <div class="row">
    <div class="teams-img text-center pt-3">
      <%= link_to "　　投　稿　　", new_post_path, class:"btn btn-lg btn-primary mt-5" %>
    </div>
  </div>

  <h2 class="text-center mt-5">投稿一覧</h2>

  <div class="col-8 mx-auto">
    <%= link_to "お気に入り順", posts_path(sort: "favorite") %>/<%= link_to "フォロー中", posts_path(sort: "follow") %>
    <%= render "posts/index", posts: @posts %>
  </div>
</div>